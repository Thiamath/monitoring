The authoritative manifests are in appcluster. mngtcluster is a symlink. If
mngtcluster-specific changes are needed, create a real directory and a symlink
to any non-changed file (manual copy-on-write :) )
